{
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "description": "文件路径"
    },
    "offset": {
      "type": "number",
      "description": "起始行（1-based，默认从头）"
    },
    "limit": {
      "type": "number",
      "description": "最多读取行数（默认全部）"
    }
  },
  "required": ["path"]
}
